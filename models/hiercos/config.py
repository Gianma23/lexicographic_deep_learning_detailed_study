# Kept as the historical import path for the Hier-COS modules; the helpers are
# shared with the other model families.
from ..common.config import parse_bool, section_to_dict

__all__ = ["parse_bool", "section_to_dict"]
