# Local machine paths. Copy this file to .env and edit the values there.
# Existing process environment variables take precedence over values in .env.
CIFAR100_ROOT=/scratch/g.saggini1/datasets/cifar100
CUB200_ROOT=/scratch/g.saggini1/datasets/CUB_200_2011
AIRCRAFT_ROOT=/scratch/g.saggini1/datasets/fgvc-aircraft-2013b
OUTPUTS_ROOT=/scratch/g.saggini1/outputs

# Shared launcher defaults. Individual commands can still override these.
PYTHON_BIN=python
MAX_PARALLEL=1
MAX_RESUME_RETRIES=1
NUM_RUNS=1
BASE_SEED=0
SPLIT_SEED=0
TRAIN_DEVICE=cuda
